#include<stdio.h>

void main()
{
    int length,width,area;
    printf("Ënter the Length and width of the reacatangle respectively: ");
    scanf("%d %d\n",&length, &width);

    area =length*width;
    printf("The area of the recatngle is: %d\n",area);
}